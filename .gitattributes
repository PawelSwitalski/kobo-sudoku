# Shell scripts and device-side files must stay LF — they run on the Kobo /
# in WSL, where CRLF breaks the shebang and kfmon's ini parser.
*.sh  text eol=lf
*.ini text eol=lf
dist/.adds/sudoku/nm-sudoku.txt text eol=lf
